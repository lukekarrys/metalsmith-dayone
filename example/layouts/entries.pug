include mixins/pagination
include mixins/entry-link

extends base

block content
  h1 Entries #{pagination.num}

  ul
    each entry in pagination.files
      li: +entryLink(entry)

  +pagination(pagination)
